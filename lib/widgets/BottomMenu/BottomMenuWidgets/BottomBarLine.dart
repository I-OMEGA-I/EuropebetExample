import 'package:flutter/material.dart';

class BottomBarLine extends StatelessWidget {
  const BottomBarLine({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 1,
      color: Color(0xff3B3D42),
    );
  }
}